
subprojects {
    apply plugin: 'java'

    sourceSets.main.java.srcDirs 'src'

    repositories {
        mavenCentral()
    }

    configurations.compile {
        exclude group: 'javax.transaction'
    }
}
